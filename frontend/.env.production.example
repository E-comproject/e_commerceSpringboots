# Production Environment Variables for Frontend
# Copy this file to .env.production and update with your actual values

# API Configuration
NEXT_PUBLIC_API_URL=https://your-backend-url.onrender.com/api
NEXT_PUBLIC_WS_URL=wss://your-backend-url.onrender.com

# Omise Payment Gateway (Production Public Key)
NEXT_PUBLIC_OMISE_PUBLIC_KEY=pkey_live_xxxxxxxxxxxxxxxxxxxxx

# Application URL
NEXT_PUBLIC_APP_URL=https://your-frontend-url.onrender.com

# Notes:
# - All variables must start with NEXT_PUBLIC_ to be accessible in browser
# - Update URLs after deploying to Render
# - Never commit .env.production to git (it's in .gitignore)
# - Use production Omise key (pkey_live_), not test key (pkey_test_)
